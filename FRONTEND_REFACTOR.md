# Swimmer_Tracker 前端重构文档

> 供后续 AI 或开发者理解视觉重构的范围、设计决策和修改约定。

## 变更文件

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `vision_app/ui_theme.py` | **新增** | 主题系统 + 4 个动画 Canvas 组件 |
| `vision_app/app_config.py` | 扩展 | IP 历史持久化 |
| `vision_app/swimming_app.py` | **重写 GUI** | 分页一屏布局，接入媒体分析与录制工作流 |
| `vision_app/video_recorder.py` | **新增** | OpenCV 录像生命周期、自定义路径和格式校验 |
| `FRONTEND_REFACTOR.md` | **新增** | 本文档 |

## 视觉设计

### 调色板 `P`

极深底色 `#04080F`，品牌色改为**薄荷绿 `#06D6A0`**（比普通青色更有水下感），所有色用 3~4 级深浅（如 `cy`/`cy2`/`cy3`/`cy4`），按钮用深底+亮字。

### 布局（v5 — 媒体工作流 + 分页控制台）

```
┌─ 顶栏 (bg2) ────────────────────────────────────────┐
│ SWIMMER_TRACKER  UNDERWATER VISION CONSOLE   [● 状态]│
├─ 水波纹动画条 (Canvas, ~24px) ────────────────────────┤
│～～～～～～～～～～～～～～～～～～～～～～～～～～～～～～│
├─ 状态横幅 ───────────────────────────────────────────┤
├─ 主体 (grid: 左视频 + 右控制台) ────────────────────┤
│ ┌─ 媒体工具栏 ─────────┐ ┌─ 分页控制台 ──────────┐ │
│ │上传·分析/暂停·录制·关闭│ │[设备输入][运动][高级] │ │
│ ├─ 视频+HUD ───────────┤ │ 当前页双列参数         │ │
│ │ 角标·十字·安全线      │ │ 监督与安全提示         │ │
│ │ 底部信息条            │ ├─ 固定操作区 ─────────┤ │
│ ├─ 仪表条 ─────────────┤ │ 框选·启动·停止·复位   │ │
│ │偏差·速度·RPM·机速·表盘│ │ 录像路径/状态          │ │
│ └───────────────────────┘ └────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

**关键改进（v5）：**
- 右列使用 Notebook 分页，不再同时堆叠三组参数，也不使用滚动条
- 媒体输入与电机连接解耦，本地视频无需连接电机即可分析
- 本地视频载入后停在首帧，先框选目标，再开始或暂停逐帧分析
- 离线播放使用时间线补偿调度，CSRT 处理耗时不再额外叠加一整帧等待；落后时有限跳帧追赶原始帧率
- 相机与离线视频均可录制带分析叠加层的结果，并自定义保存位置
- 操作按钮（框选/启动/停止/复位）固定在右列底部
- RPM 转速表从右侧移至底部仪表条，与 4 个指标卡并排
- 底部仪表条跨左列全宽，信息密度更高

### 动画组件

| 组件 | 类/函数 | 说明 |
| --- | --- | --- |
| 水波纹 | `WaveBar` | Canvas 绘制两层正弦波，`after()` 驱动，45ms 刷新，营造水下氛围 |
| 脉冲灯 | `PulseDot` | 状态圆点+光晕呼吸动画，运行/故障时脉冲，停止时静态 |
| 转速表 | `ArcGauge` | 270° 弧 + 27 条刻度线 + 4 个刻度标签，前景弧按 RPM 占比填充，分段着色（<70%绿 <90%琥珀 ≥90%红） |
| HUD 叠加 | `draw_hud()` | 四角 L 形线标 + 双层十字准线 + 中心圆 + 底部半透明信息条 + 安全区虚线 |

### 指标卡片 `metric_card()`

左侧 3px 竖强调条（颜色可选）+ 灰标题 + 20 号等宽大数值。

### IP 历史

`load_tcp_host_history()` / `save_tcp_host_history()`，存于 `tcp_host_history.json`，最多 8 条，默认含 `192.168.1.177`。连接 TCP 成功后自动保存。

## 修改约定

1. 颜色只改 `P`，不在其他文件硬编码色值
2. 新 ttk Style 先在 `ui_theme.py` 注册
3. 测试兼容：`backend_badge_var` 含 `"virtual"`、`open_camera_button` state、`camera_source_box` values 必须保留
4. 动画组件返回 `(widget, update_fn)` 或自有 `.widget` 属性，由 `swimming_app.py` 的 tick/refresh 方法调用
5. IP 历史与 `ControlSettings` 解耦
6. 右列不再滚动——新增控件须放入对应 Notebook 分页，操作区始终固定在底部
7. 离线分析不依赖电机安全状态；真实 TCP 电机仍禁止使用本地视频驱动
8. 录像必须通过 `VideoRecorder` 管理，所有关闭、故障、结束和退出路径都要释放 writer

## 验证

```powershell
.venv\Scripts\python.exe -m unittest discover -s vision_app/tests -v
.venv\Scripts\python.exe vision_app\swimming_gui.py
```

手动检查：深色主题、水波纹动画、HUD 角标/十字/安全线、RPM 表动态填充、脉冲灯、IP 历史、按钮语义色、状态横幅切换、右列三个分页切换且无滚动条。
另检查：上传视频停在首帧、无需电机即可框选、分析暂停/继续、自定义录像路径、停止/视频结束后文件可播放、三个控制台分页均无截断。
